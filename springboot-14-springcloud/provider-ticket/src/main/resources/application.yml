server:
  port: 8001

spring:
  application:
    name: provider-ticket
#eureka
eureka:
  instance:
    prefer-ip-address: true #注册服务的时候使用服务的IP地址
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #eureka服务配置地址