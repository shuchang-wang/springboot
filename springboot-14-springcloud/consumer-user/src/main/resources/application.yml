server:
  port: 8200
#应用名配置
spring:
  application:
    name: consumer-user
#eureka配置
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/   #eureka服务配置地址